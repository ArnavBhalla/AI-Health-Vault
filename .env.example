# Database
DATABASE_URL="file:./dev.db"

# Session secret (generate a random string for production)
SESSION_SECRET="change-this-to-a-random-secret-in-production"

# Storage
STORAGE_PATH="./storage"

# AI Settings
AI_PROVIDER="mock" # mock | anthropic | openai
ANTHROPIC_API_KEY="" # Add when ready to use Claude
OPENAI_API_KEY="" # Add as fallback

# Feature flags
ENABLE_PHI_TO_AI="false" # Keep false to de-identify data sent to AI
ENABLE_WEBAUTHN="false" # Enable when adding passkey support

# App
NEXT_PUBLIC_APP_NAME="AI Health Vault"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
